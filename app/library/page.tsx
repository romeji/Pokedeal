import { prisma } from "@/lib/database/prisma";
import { ScoreBadge } from "@/components/dashboard/ScoreBadge";
export const dynamic="force-dynamic";
export default async function LibraryPage(){let rows:any[]=[];try{rows=await prisma.opportunity.findMany({where:{estimatedProfit:{gt:0},listing:{status:{notIn:["SOLD","REMOVED","EXPIRED"]}}},include:{listing:true,score:true},orderBy:[{estimatedProfit:"desc"},{updatedAt:"desc"}],take:200})}catch{}
return <main className="p-5 md:p-10"><p className="eyebrow">Bibliothèque active</p><h1 className="mt-2 font-display text-4xl font-bold">Deals rentables</h1><p className="mt-2 text-slate-400">Uniquement les annonces avec profit positif qui ne sont ni vendues, ni supprimées, ni expirées.</p><div className="mt-8 grid gap-4 lg:grid-cols-2 xl:grid-cols-3">{rows.map(o=><a href={o.listing.url} target="_blank" rel="noreferrer" key={o.id} className="surface group p-5 transition hover:-translate-y-1 hover:border-cyan-300/40"><div className="mb-4 flex justify-between gap-3"><h2 className="font-semibold group-hover:text-cyan-200">{o.listing.title}</h2>{o.score&&<ScoreBadge score={o.score.score}/>}</div><div className="grid grid-cols-3 gap-3 text-sm"><div><span className="text-slate-500">Prix</span><b className="block">{Number(o.purchasePrice).toFixed(2)} €</b></div><div><span className="text-slate-500">Marché</span><b className="block">{Number(o.marketValue??0).toFixed(2)} €</b></div><div><span className="text-slate-500">Profit</span><b className="block text-emerald-300">+{Number(o.estimatedProfit??0).toFixed(2)} €</b></div></div><div className="mt-4 text-xs text-slate-500">ROI {o.roi??0}% · actualisé {o.listing.lastSeenAt.toLocaleString('fr-FR')}</div></a>)}{!rows.length&&<div className="surface p-10 text-slate-400">La bibliothèque est vide pour le moment.</div>}</div></main>}
